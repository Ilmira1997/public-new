import { SCLoader,Container } from "./loader.styled";
export const Loader = () => {
    return (
        <Container>
            <SCLoader className="loader"></SCLoader>
        </Container>
    );
};
